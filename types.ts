/**
 * types and constants
 */

export enum Color {
  Red = 0, Yellow, Orange, White, Blue, Green
}

export enum Move {
  F, Fi, R, Ri, U, Ui, B, Bi, L, Li, D, Di
}

export enum Face {
  Top, Front, Back, Bottom, Left, Right
}

